# Read about fixtures at http://ar.rubyonrails.org/classes/Fixtures.html

open:
  name: open
  title: An open boards
  open_level: <%= OpenLevel::OPEN %>

read_only:
  name: read_only
  title: A Read-only board
  open_level: <%= OpenLevel::READ_ONLY %>

list_only:
  name: list_only
  title: A List-only board
  open_level: <%= OpenLevel::LIST_ONLY %>

closed:
  name: closed
  title: A Closed board
  open_level: <%= OpenLevel::CLOSED %>
