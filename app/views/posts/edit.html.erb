<%= page_title "포스트 수정" %>

<% airbudd_form_for [@board, @post] do |form| %>
	<%= form.label_suffix = '' %>
	<%= form.text_field :title, :size => 70, :label => '제목' %>
	<%= form.text_area :body, :cols => 107, :label => '' %>
	<% if logged_in? %>
		<%= form.check_box :notice, :label => APP_CONFIG[:notice] %>
	<% end %>	
	<%= form.submit '저장' %>
	<%= link_to '취소', :back %>
<% end %>